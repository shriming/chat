block('dialog').elem('console')(
    mix()('i-bem'),
    js()(true),
    content()(
        function () {
            return [
                {
                    block : 'textarea',
                    mods : { theme : 'shriming', focused : true, name : 'msg' },
                    placeholder : 'Ваш текст должен быть здесь'
                }
            ];
        }
    )
);
